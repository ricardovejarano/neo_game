# Neo Mundo Travel

Azar Game for clients

## Basic Usage

```shell
git clone
cd angular-electron
npm install

# run the app
npm run electron-build

# build the app
npm install electron-packager -g
electron-packager . --platform=win32
```
